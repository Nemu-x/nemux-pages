---
import { cn, withBase } from '~/lib/utils'

type Props = {
  href: string
  class?: string
  [key: string]: any
}

const { href, class: className, ...props }: Props = Astro.props
const isExternal = href.startsWith('http')
const resolvedHref = isExternal ? href : withBase(href)
---

<a
  href={resolvedHref}
  class={cn('transition text-muted-foreground/80  hover:text-zinc-900 dark:hover:text-white', className)}
  target={isExternal ? '_blank' : undefined}
  rel={isExternal ? 'noopener noreferrer' : undefined}
  {...props}
>
  <slot />
</a>
